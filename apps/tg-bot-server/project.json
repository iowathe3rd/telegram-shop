{
  "name": "tg-bot-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/tg-bot-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/tg-bot-server",
        "main": "apps/tg-bot-server/src/main.ts",
        "tsConfig": "apps/tg-bot-server/tsconfig.app.json",
        "assets": [
          "apps/tg-bot-server/src/assets"
        ],
        "webpackConfig": "apps/tg-bot-server/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "tg-bot-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "tg-bot-server:build:development"
        },
        "production": {
          "buildTarget": "tg-bot-server:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/tg-bot-server/jest.config.ts"
      }
    }
  },
  "tags": []
}
